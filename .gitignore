# Data (too large for git)
файлы/
data/raw/
*.parquet
*.pkl
*.pickle
*.joblib
*.zip
presentation.pdf

# Keep ONLY the 3 core MVP models (cascade: CRF NER + brand clf + attr typer).
# Everything else under models/ and every artifacts/*.joblib stays local. See models/README.md.
!models/ner_crf.pkl
!models/brand_clf.joblib
!models/attr_type_clf.joblib
# local-only baselines / experiment dumps under models/
models/*.json

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
dist/
build/
.venv/
venv/
.env

# Jupyter
.ipynb_checkpoints/
*/.ipynb_checkpoints/

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db
desktop.ini

# Models / caches (large)
models/checkpoints/
*.pt
*.bin
*.onnx
.cache/
huggingface/

# Keep figures and small artifacts
!figures/
!docs/figures/

# LaTeX build
*.aux
*.log
*.out
*.toc
*.synctex.gz
*.fls
*.fdb_latexmk
# keep documentation PDF in repo
# docs/*.pdf

# Temp
tmp/
temp/
*.tmp

# Приватный ключ подписи кода — .cer (публичный сертификат) можно коммитить,
# .pfx (приватный ключ) — никогда
installer/certs/*.pfx
